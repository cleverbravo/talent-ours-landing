import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['192.168.100.126','*','doom-intention-videos-icon.trycloudflare.com','talentours.com'],
};

export default nextConfig;
